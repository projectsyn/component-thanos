parameters:
  kapitan:
    compile:
      - input_paths:
          - thanos/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - thanos/component/main.jsonnet
        input_type: jsonnet
        output_path: ${_instance}
